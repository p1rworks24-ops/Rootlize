Stored facts (source of truth). Judge only from these facts. List every independent condition the query states. Do not add extra conditions. Named surfaces are one condition. Library-search wrappers such as images/screenshots/from this folder are not conditions. Bare named-entity queries need sufficient identity presence (rendered UI, clear icon, clear shortcut, or nested screenshot), not a weak text or folder-name mention. Do not require a presence form the query did not name. Workspace queries need that identity's page/pane/panel. Do not output a final relevant flag.
